module demo.stdlib_playbook.strings;

fn title_line() -> Str
describe "Return a normalized title line"
tags ["stdlib", "string"]
requires []
ensures [starts_with(result, "Title:")]
{
  let raw = "  kairos   platform  ";
  return concat("Title: ", upper(normalize_space(raw)));
}
